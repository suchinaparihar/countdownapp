import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; //Current directory that is why '.' is used before the name

ReactDOM.render(
  <App> </App>, document.getElementById('root')
);
